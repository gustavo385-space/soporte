# Copia este archivo a ".env" (sin ".example") y completa tu propia cadena de
# conexión de Supabase. El archivo ".env" NUNCA debe subirse a git.
#
# La encuentras en: Supabase Dashboard -> Project Settings -> Database
# -> Connection string -> modo "Transaction" (puerto 6543) o "Session" (5432).
#
# IMPORTANTE: como acabas de compartir tu contraseña anterior en un chat,
# genera una nueva desde "Reset Database Password" antes de usarla aquí.

DATABASE_URL=DATABASE_URL=postgresql://postgres.jlenmavcvkinxdphndsr:Tavo200508xd%40@aws-0-us-east-2.pooler.supabase.com:6543/postgres

PORT=3000
